import React from 'react';
import ReactDOM from 'react-dom';
import ProductosComponent from './diccionario.jsx';

ReactDOM.render(
  <React.StrictMode>
    <ProductosComponent />
  </React.StrictMode>,
  document.getElementById('root')
);